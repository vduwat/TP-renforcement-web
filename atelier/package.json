{
  "name": "cap-web-atelier",
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=24.20.0"
  },
  "scripts": {
    "start": "node server/start.js",
    "test": "node --test tests/*.test.js",
    "lint": "eslint .",
    "test:browser": "playwright test",
    "verify": "npm run lint && npm test && npm run test:browser"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.13.0",
    "@playwright/test": "1.63.0",
    "eslint": "10.10.0"
  }
}
